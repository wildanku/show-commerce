import Layout from '@/Components/section/Layout';
import { PageProps } from '@/types';

export default function Dashboard({ auth }: PageProps) {
  return <div className="w-full h-screen bg-purple-800"></div>;
}
